# Images

Put local preview images here only if they are approved for public display.

For production, add cover image IDs to `QEF_Pages.封面圖片ID` or folder IDs to `QEF_Pages.資料夾ID` in the Google Sheet backend.
